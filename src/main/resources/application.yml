spring:
  application:
    name: service-platform
  datasource:
    url: jdbc:mysql://database-service.chqmykiua4ev.eu-west-3.rds.amazonaws.com:3306/servicedb
    username: admin
    password: mypassword
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

server:
  port: 8081
